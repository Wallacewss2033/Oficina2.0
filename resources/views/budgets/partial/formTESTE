<div>

    <form>

        <div class="form-row">

            <div class="form-group col-md-6">
                <label for="inputEmail4">Cliente</label>
                @if(isset($teste))
                <input type="text" class="form-control" id="inputEmail4" placeholder="Nome completo" value="{{$teste}}">
                @else
                <input type="text" class="form-control" id="inputEmail4" placeholder="Nome completo">
                @endif
            </div>

            <div class=" form-group col-md-6">
                <label for="inputAddress">Vendedor</label>
                @if(isset($teste))
                <input type="text" class="form-control" id="inputAddress" placeholder="Nome completo" value="{{$teste}}">
                @else
                <input type="text" class="form-control" id="inputAddress" placeholder="Nome completo">
                @endif
            </div>
        </div>

        <div class="form-row">
            <div class="form-group col-md-4">
                <label for="inputPassword4">Data do orçamento</label>
                @if(isset($teste))
                <input type="date" class="form-control" id="inputPassword4">
                @else
                <input type="date" class="form-control" id="inputPassword4" value="{{$teste}}">
                @endif
            </div>
            <div class="form-group col-md-4">
                <label for="inputPassword4">horario do orçamento</label>
                @if(isset($teste))
                <input type="time" class="form-control" id="inputPassword4">
                @else
                <input type="time" class="form-control" id="inputPassword4" value="{{$teste}}">
                @endif
            </div>

            <div class="form-group col-md-4">
                <label for="inputZip">Valor</label>
                @if(isset($teste))
                <input type="text" class="form-control" id="inputZip" placeholder="00,00">
                @else
                <input type="text" class="form-control" id="inputZip" placeholder="00,00" value="{{$teste}}">
            </div>
        </div>

        <div class="form-group">
            <div class="form-group">
                <label for="exampleFormControlTextarea1">Descrição</label>
                @if(isset($teste))
                <textarea class="form-control" id="exampleFormControlTextarea1" rows="3">{{$teste}}</textarea>
                @else
                <textarea class="form-control" id="exampleFormControlTextarea1" rows="3"></textarea>
                @endif

            </div>
        </div>


        <button type="submit" class="btn btn-primary">Sign in</button>
    </form>
</div>




$obj2=new stdClass();
    $obj2->id = 2;
    $obj2->cliente = 'Matheus Vilarue';
    $obj2->vendedor = "Lucas Ruan";
    $obj2->datatime = date(now());
    $segments = Str::of($obj2->datatime)->split('/[\s,]+/');
    $obj2->time = $segments[1];
    $obj2->data = $segments[0];
    $obj2->valor = "100,00";
    $obj2->description = "Ame, como se ninguém nunca houvesse feito você sofrer. Trabalhe, como se não precisasse do dinheiro. Dance, como se ninguém estivesse olhando. Cante, como se ninguém estivesse ouvindo. Viva, como se fosse no paraíso!

    Curta o que de melhor a vida lhe oferece com toda intensidade, como se fosse o último dia da sua vida. A vida muitas vezes é curta, mas mesmo assim seu caminho é longo. Nela aprendemos a sorrir, chorar, amar, sofrer e a renascer para amanhecer e termos um lindo dia.
    
    Não deixe para amanhã o que pode ser feito hoje; o ontem já passou e o amanhã talvez não chegue. Seja Feliz Sempre!";